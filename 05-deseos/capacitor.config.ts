import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dogdev.deseos',
  appName: 'Deseos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
